{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Start bot",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/victory-day-bot/main.go"
        }
    ]
}